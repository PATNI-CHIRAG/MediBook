@model HospitalAppointmentSystem.Models.Patient

@{
    ViewData["Title"] = "Register";
}

<style>

    .auth-container {
        min-height: 80vh;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .auth-card {
        width: 100%;
        max-width: 420px;
        border-radius: 12px;
        transition: 0.3s;
    }

        .auth-card:hover {
            transform: translateY(-4px);
        }

    .auth-input {
        border-radius: 8px;
    }

    .auth-btn {
        border-radius: 8px;
        transition: 0.3s;
    }

        .auth-btn:hover {
            transform: scale(1.02);
        }

</style>


<div class="container auth-container">

    <div class="card shadow-lg p-4 auth-card">

        <h3 class="fw-bold">Create Account</h3>
        <p class="text-muted">Please sign up to book appointment</p>

        <form asp-action="Register" method="post">

            <div class="mb-3">
                <label class="form-label">Full Name</label>
                <input asp-for="Name" class="form-control auth-input" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Email</label>
                <input asp-for="Email" class="form-control auth-input" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Phone</label>
                <input asp-for="Phone" class="form-control auth-input">
            </div>

            <div class="mb-3">
                <label class="form-label">Password</label>
                <input name="password" type="password" class="form-control auth-input" required>
            </div>

            <button class="btn btn-primary w-100 auth-btn">
                Create Account
            </button>

            <p class="mt-3 text-center">

                Already have an account?

                <a asp-controller="Account"
                   asp-action="Login"
                   class="text-primary fw-semibold">

                    Login here

                </a>

            </p>

        </form>

    </div>

</div>